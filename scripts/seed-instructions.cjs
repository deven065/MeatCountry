console.log(`\nSupabase seed instructions:\n\n1) Create a new Supabase project at https://supabase.com/dashboard\n2) Copy .env.local.example to .env.local and fill NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY\n3) In Supabase SQL editor, paste contents of ./supabase/schema.sql and run it\n4) In Table Editor, import ./supabase/seed.json into the products table (JSON import)\n\nNote: Public policies allow read for anon. Lock down write access via RLS as needed.\n`)

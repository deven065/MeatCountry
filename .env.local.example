NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# WARNING: Do NOT expose the service role key to the browser.
# If you need admin operations on the server (e.g., seeding,
# secured inserts/updates, revalidation), set this ONLY as a
# non-public env var without NEXT_PUBLIC, and access it in
# server-only code or serverless functions.
# Example (do not commit real secrets):
# SUPABASE_SERVICE_ROLE=
